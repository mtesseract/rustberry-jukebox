apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jukebox-audio
  annotations:
    volumeType: local
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: jukebox-audio
  resources:
    requests:
      storage: 1Gi
