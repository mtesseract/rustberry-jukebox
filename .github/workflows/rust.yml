name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install Dependencies
      run: sudo apt-get install --yes curl gcc gcc-multilib-arm-linux-gnueabihf pkg-config libssl-dev \
    - name: Build
      run: cd jukeboxd && cargo build --verbose
    - name: Run tests
      run: cd jukeboxd && cargo test --verbose
